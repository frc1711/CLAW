package rct.commands.config;

import rct.low.InstructionMessage;

public class ListConfigInstruction extends InstructionMessage {
    public ListConfigInstruction (int id) {
        super(id);
    }
}

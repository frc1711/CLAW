
/**
 * This package contains code used for both the driverstation Robot Control Terminal (RCT) app and the roboRIO
 * RCT Server. It should be entirely self contained, as it is shared by both projects (i.e. it should not reference
 * the rest of the {@code claw.***} packages). This package is almost entirely used internally by CLAW and the
 * RCT driverstation app.
 */
package claw.rct.base;
